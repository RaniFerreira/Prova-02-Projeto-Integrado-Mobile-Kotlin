package com.example.apptreino.ui.theme

import androidx.compose.ui.graphics.Color

val BgApp = Color(0xFFFBF9F6) // fundo geral das telas
val Surface = Color(0xFFFFFFFF) // cards, top bar
val Primary = Color(0xFFE84D1C) // laranja - ações principais, FAB, destaque de km
val TextPrimary = Color(0xFF1C1A17)
val TextMuted = Color(0xFF736E66) // legendas, datas, labels
val BorderCard = Color(0xFFE0DBD4) // stroke sutil dos cards
val ChipBg = Color(0xFFF2EDE6) // fundo de chip inativo / campo de busca / inputs
val Success = Color(0xFF298C4D) // usado no pace da corrida
